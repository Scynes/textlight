import { Button } from '@radix-ui/themes';

export const FeedbackButton = () => {
    return (
        <Button variant={ 'outline' } className={ 'cursor-pointer transition-all' }>
            Feedback
        </Button>
    );
}